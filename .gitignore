# 認証情報。サーバーごとに作成するため、リポジトリには含めない。
.htpasswd

# Googleアカウント認証の設定。クライアントIDとクライアントシークレットが
# 入るため、リポジトリには含めない。
# auth-config.php.example をコピーして作成する。
/auth-config.php

# デプロイ設定。接続先はサーバーごとに違うため、リポジトリには含めない。
# Makefile.example をコピーして作成する。
Makefile

# Apache の設定。認証情報の置き場所（絶対パス）がサーバーごとに違うため、
# リポジトリには含めない。.htaccess.example をコピーして作成する。
# （media/ の .htaccess は環境に依存しないので対象外）
/.htaccess

# 写真・動画データ。サイズが大きいためリポジトリでは管理しない。
media/*
!media/.gitkeep
!media/.htaccess

data/*

.DS_Store
*.sh
